<menu>
  <%= link_to "Home", root_path %>

  <div class="user-menu-wrapper">
    <% if user_signed_in? %>
      <span class="user">
        <%= full_name(current_user) %>
      </span>
      <ul class="user-menu">
        <li><%= link_to "Accounts", '#' %></li>
        <li><%= link_to "Profile", current_user %></li>
        <li><%= link_to "Help", '#' %></li>

        <%# if !current_user.acumen_tests.any? %>
          <li><%= link_to "Create Acumen Test", new_acumen_test_path %></li>
        <%# elsif current_user.acumen_tests.any? && !current_user.acumen_tests.first.finished? %>
          <!-- <li><%#= link_to "Continue Acumen Test", edit_acumen_test_path(current_user.acumen_tests.first) %></li> -->
        <%# else %>
          <!-- <li><%#= link_to "Acumen Test Result", acumen_test_path(current_user.acumen_tests.first) %></li> -->
        <%# end %>

        <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>

      </ul>

    <% else %>
      <%= link_to "Sign in", user_session_path %>
      or
      <%= link_to "Sign up", new_user_registration_path %>
    <% end %>
  </div>
</menu>
